# Copy these environment variables to Railway Dashboard → Your Project → Variables

# Required API Keys
TELEGRAM_BOT_TOKEN=<set-in-railway>
OPENROUTER_API_KEY=<set-in-railway>
TWELVEDATA_API_KEY=<set-in-railway>
CHARTIMG_API_KEY=<set-in-railway>
GOOGLE_SHEETS_ID=<optional-google-sheet-id>

# App Configuration  
APP_NAME=Legend AI
DEBUG=false
SECRET_KEY=<generate-a-strong-secret>

# Database & Redis (Railway provides these automatically)
# DO NOT SET THESE MANUALLY - Railway sets them automatically
DATABASE_URL=${{ PostgreSQL.DATABASE_URL }}
REDIS_URL=${{ Redis.REDIS_URL }}

# Optional (leave empty for now)
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
